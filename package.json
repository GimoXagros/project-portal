{
  "name": "project-portal",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "oxlint",
    "validate:data": "node scripts/validate-data.mjs",
    "verify:releases": "node scripts/verify-releases.mjs",
    "test": "node --test scripts/tests/*.test.mjs",
    "check": "npm run validate:data && npm run verify:releases && npm run lint && npm run build",
    "preview": "vite preview"
  },
  "dependencies": {
    "lucide-react": "^0.468.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@types/react": "^19.1.16",
    "@types/react-dom": "^19.1.9",
    "@vitejs/plugin-react": "^5.0.4",
    "oxlint": "^1.17.0",
    "vite": "^7.1.7"
  }
}
